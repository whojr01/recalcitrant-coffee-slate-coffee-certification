{% comment %}
 usage: render 'video-settings.liquid', videoSources:section.settings.video_desktop.sources
 Output: Source URL, Source Format, START, ...
{% endcomment %}

{%- for source in videoSources -%}
    {%- unless forloop.first -%}
        START, 
    {%- endunless -%}
    {{ source.url }},{{ source.format }}

    {%- unless forloop.last -%}
        ,
    {%- endunless -%}
{%- endfor -%}